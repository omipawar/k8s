kind: PersistentVolume
apiVersion: v1
metadata:
  name: pv1
  namespace: pv
spec:
  capacity: 
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: /mnt/data
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
