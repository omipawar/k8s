kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc1
  namespace: pv
spec: 
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-storage
